# Swisseph WASM

Place your compiled `swisseph.js` and `swisseph.wasm` files in this directory.
The library expects an `ephe` folder here containing ephemeris data files. A
minimal `package.json` is provided so that `require('swisseph')` resolves to
these files.
